/*! JSZip stub. For full ZIP export, replace this file with jszip.min.js from https://stuk.github.io/jszip/ */
class JSZip {
  folder(){ return this; }
  file(){}
  async generateAsync(){ alert('ZIP export requires JSZip. Please replace jszip.min.js with the real file. Falling back to CSV export.'); return new Blob(['']); }
}
